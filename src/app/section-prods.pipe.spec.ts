import { SectionProdsPipe } from './section-prods.pipe';

describe('SectionProdsPipe', () => {
  it('create an instance', () => {
    const pipe = new SectionProdsPipe();
    expect(pipe).toBeTruthy();
  });
});
